apiVersion: apps/v1
kind: Deployment
metadata:
  name: loan-service
  namespace: homestarter-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loan-service
  template:
    metadata:
      labels:
        app: loan-service
    spec:
      imagePullSecrets:
        - name: homestarter
      containers:
        - name: loan-service
          image: docker.io/daewoongjeon/loan-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: cm-common
            - configMapRef:
                name: cm-loan-service
            - secretRef:
                name: secret-common
            - secretRef:
                name: secret-loan-service
          resources:
            requests:
              cpu: 256m
              memory: 256Mi
            limits:
              cpu: 256m
              memory: 256Mi
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 15
            failureThreshold: 3
