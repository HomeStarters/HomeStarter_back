apiVersion: apps/v1
kind: Deployment
metadata:
  name: asset-service
  namespace: homestarter-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: asset-service
  template:
    metadata:
      labels:
        app: asset-service
    spec:
      imagePullSecrets:
        - name: homestarter
      containers:
        - name: asset-service
          image: docker.io/daewoongjeon/asset-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: cm-common
            - configMapRef:
                name: cm-asset-service
            - secretRef:
                name: secret-common
            - secretRef:
                name: secret-asset-service
          resources:
            requests:
              cpu: 256m
              memory: 512Mi
            limits:
              cpu: 256m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 15
            failureThreshold: 3
